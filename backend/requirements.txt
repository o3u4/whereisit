fastapi>=0.115
uvicorn[standard]>=0.30
cryptography>=43
pydantic>=2.7
python-multipart>=0.0.9  # UploadFile/Form (image upload)
openai>=1.50  # LLM parse (OpenAI-compatible multimodal)
anthropic>=0.40  # Claude (Anthropic Messages API, not OpenAI-compatible)
