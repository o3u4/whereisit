该项目旨在做一个记录，搜索，更新 物品所在位置的app，适应多种场景，如家居卧室，办公室，再到细小结构，比如衣柜，桌面，桌面柜子等等，可以将物品所在路径看作“系统文件夹”路径，层层往下，当然，不同场景可以分开，也可以套娃。

项目完整功能包括但不限于存储物品位置（可以通过多种方式），登记各条目到数据库，选择怎么样的数据结构你得自由发挥，后续可能出现的搜索方式包括精确搜索（环境，物品，编号等全部确定），模糊搜索，类别搜索（搜索相似类别的物品），存在性查找（给一个可能存在的目标，去指定范围内查找是否真的存在），后续版本迭代更新可能会有更高级的搜索方式，比如向量化搜索等等，你可以预留一个心眼，防止后续没法兼容前面的工作。更新阶段，逻辑与第一次存储场景比较类似，你可以看着完成。

注意存储阶段，可能需要面对非常多复杂情况，比如给定场景，可能需要先分析其现有结构，比如一个桌子有几个柜子，具体方位如何，左边几个，右边几个。需要从语言，甚至是照片中读取出结构信息，每个场景下的结构肯定大有差别，这是一个难点，根据不同场景如何合理设置各自的属性（检索的时候没准就是用的属性搜索）非常有考究。这里如果一般方案实在做不到或者没有性价比，可以考虑调用现成大模型的api来完成，你需要编写好系统提示词，让其成功完成相应功能，api需要再设置里配置。

关于前端交互设计，你可以查看使用合适的skill来完成，尽量设计地高端简洁，比如apple的审美，考虑更高效，美观的交互设计，比如将已经存储好的场景整体拖动到另一个场景中作为子空间存储，等等。

后续还要添加其它功能，在完成这些基础功能的前提下，预留更新迭代的空间。最好功能用模块化实现，便于维护与更新。

适用平台，目前开发于浏览器服务，即通过浏览器即可访问本地服务。后续需要迁移至windows，Android平台，开发客户端，如果迁移方案工作量较大，你可以给出更优的方案。

